<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sawt_al_amal">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.LessonsActivity"/>
        <activity android:name=".activity.signup" />
        <activity android:name=".activity.login" />
        <activity android:name=".activity.HomeActivity" />
        <activity android:name=".activity.MainActivity1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GifsActivity" />
        <activity android:name=".activity.soundRec.vr.record.Testing" />
        <activity android:name=".activity.soundRec.vr.record.SettingsActivity" />
        <activity
            android:name=".activity.soundRec.vr.record.MainActivity2"
            android:configChanges="keyboard|orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".activity.soundRec.vr.record.Licenses" />
        <activity android:name=".activity.soundRec.vr.record.recorder.RecordActivity" />
        <activity android:name=".activity.soundRec.vr.record.list.RecordingsListActivity" />
    </application>

</manifest>